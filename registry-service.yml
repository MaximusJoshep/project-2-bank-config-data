server:
    port: 8091
eureka:
    instance:
       hostname: localhost
    client:
       registerWithEureka: false
       fetchRegistry: false
       serviceUrl:
          defaultZone: htt://${eureka.instance.hostname}:${server.port}
   
